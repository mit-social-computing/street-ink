import {
  feetToMiles
} from 'street-ink/helpers/feet-to-miles';

module('FeetToMilesHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = feetToMiles(42);
  ok(result);
});
