import {
  unixToCalendar
} from 'street-ink/helpers/unix-to-calendar';

module('UnixToCalendarHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = unixToCalendar(42);
  ok(result);
});
