<div class="l-flexcontent">
    <section class="l-sidebar">
        {{drop-down 
            action='setCity'
            selectedCity=selectedCity
            cities=cities}}

        <h2 class="text text--large">{{selectedCity.name}}</h2>

        {{view 'streetlist'}}
    </section>
    <section class="l-flexarea">
    </section>
</div>
